import React from "react";
// , { useContext, useEffect }
// import noteContext from "../context/notes/noteContext";

const About = () => {
  // const a = useContext(noteContext);
  // useEffect(() => {
  //   a.update();
  // });

  return (
    <div>
      {/* About {a.state.name} of {a.state.class} */}
      About
    </div>
  );
};

export default About;
